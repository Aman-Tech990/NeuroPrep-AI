import React from "react";

const FinalResult = ({ result }) => {

    const markdownComponent = {
        
    }

    return (
        <div className="">


        </div>
    );
};

export default FinalResult;